package com.stacktrace.yo.scrapeline.engine.file

import akka.actor.{Actor, ActorLogging}

class FileWriteActor extends Actor with ActorLogging{


  override def receive: Receive = {
    case _ =>
  }
}